<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.brainict.smartwave">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name = "android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <application
        android:name="com.brainict.smartwave.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.brainict.smartwave.activity.AlarmActivity"></activity>
        <activity
            android:name="com.brainict.smartwave.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.brainict.smartwave.activity.MainActivity" />

        <service android:name="com.brainict.smartwave.service.MusicService" />
        <service android:name="com.brainict.smartwave.service.WakeUpService" />

        <receiver android:name="com.brainict.smartwave.broadcastReceiver.MusicReceivers"
            android:enabled="true">
            <intent-filter>
                <action android:name="play_music_action" />
                <action android:name="wake_up_action" />
                <action android:name="in_30_awakening_action" />
                <action android:name="after_5_awakening_action" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver android:name="com.brainict.smartwave.broadcastReceiver.CallReceivers">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>